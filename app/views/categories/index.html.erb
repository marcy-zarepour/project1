<div class='homebanner'>
   <div class="row" >
   <% @categories.each do |category| %>
      <div class='col-md-4 meals'>
         <h2> <%= category.name %> </h2>
      <%= link_to image_tag(category.image,:class => 'img-fluid'), category %>
      </div>
   <% end %>
   </div>
</div>







